[project]
name = "ats-risk-rules"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "ats-contracts",
  "pydantic==2.12.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
